let students = ["Bob", "James", "Mat", "Hubert"]
for (const element of students) {
    console.log(element)
    if (element === "Mat") {
        console.log("Mat is the best student in the world");
    }
    }